// UI render only. no fetch logic, no REST here.

// import { useExamples } from "../api/useExamples";

export default function ExampleComponent() {
  // const { data, isLoading } = useExamples();

  return (
    <div>
      <h2>Example Component</h2>
    </div>
  )
}
